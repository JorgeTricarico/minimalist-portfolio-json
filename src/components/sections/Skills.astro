---
import SkillsVariant from './variants/SkillsMarquee.astro';
import type { Skills as SkillsType } from "@/cv"

interface Props {
  skills?: Array<SkillsType>
  lang?: string
}

const { skills, lang = 'es' } = Astro.props
---

<SkillsVariant skills={skills} lang={lang} />
